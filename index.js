function breakOut(array, changeValue, stopValue) {
  array[i] === changeValue;
  for (let i = 0; i < array.length; i++) {
    if (array[i] === stopValue) {
      break;

    }
    return array;


  }
}
